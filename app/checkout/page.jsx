import Checkout from "@/components/checkout/Checkout";
import React from "react";

export const metadata = {
  title: "Check Out",
};

const page = () => {
  return <Checkout />;
};

export default page;
